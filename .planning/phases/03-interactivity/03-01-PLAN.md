---
phase: 03-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mod_MapBuilder.bas
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04

must_haves:
  truths:
    - "Clicking a marker opens a popup table of all fields except Lat and Lon"
    - "A blue semi-transparent circle appears centered on the clicked marker, sized by the slider value"
    - "Non-neighbor markers fade to fillOpacity 0.15; neighbor markers remain at 0.85"
    - "Same-ISP neighbors show white border weight 3; other-ISP neighbors show weight 1"
    - "The selected marker expands to radius 12, white fill, ISP color border, weight 2"
    - "Dragging the radius slider updates the label text and redraws the circle + highlights live"
    - "Clicking the map background resets all markers, removes the circle, and closes the popup"
    - "Clicking the currently-selected marker again clears selection (toggle deselect)"
  artifacts:
    - path: "src/mod_MapBuilder.bas"
      provides: "BuildMapHtml() with interactivity JS block"
      contains: "function haversine"
    - path: "src/mod_MapBuilder.bas"
      provides: "radius slider Leaflet control"
      contains: "radiusControl"
    - path: "src/mod_MapBuilder.bas"
      provides: "clearSelection and applySelection functions"
      contains: "function clearSelection"
  key_links:
    - from: "markers[i]"
      to: "companies[i]"
      via: "parallel array — never splice, removeLayer/addLayer only"
      pattern: "markers.forEach.*companies\\[i\\]"
    - from: "L.circle"
      to: "slider value"
      via: "sliderValue * 1609.34 (miles to meters)"
      pattern: "1609\\.34"
    - from: "marker.setRadius"
      to: "marker.setStyle"
      via: "called separately — setStyle does not update radius"
      pattern: "setRadius\\(12\\)"
---

<objective>
Add all marker click interactivity and the radius slider to BuildMapHtml() in mod_MapBuilder.bas.

Purpose: Implements INT-01 through INT-04 — the core use case of clicking a company and seeing nearby ISP neighbors.

Output: mod_MapBuilder.bas extended with two JS blocks appended inside BuildMapHtml(): (1) state variables and interaction functions, (2) the radius slider Leaflet control and event bindings.
</objective>

<execution_context>
@C:\Users\TAPPo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\TAPPo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactivity/03-CONTEXT.md
@.planning/phases/03-interactivity/03-RESEARCH.md

<interfaces>
<!-- Key contracts from existing mod_MapBuilder.bas the executor must match -->

Existing globals in scope after the Phase 2 marker loop (all available to new JS):
  var companies = [...];         // JSON array from VBA jsonStr parameter
  var ISP_COLORS = [...];        // 7-element palette
  var OTHER_COLOR = "#999999";
  var ispNames = [...];          // sorted unique ISP names
  var ispColorMap = {};          // isp -> color
  function getColor(isp) { ... } // returns ispColorMap[isp] || OTHER_COLOR
  var map = L.map('map')...;
  var clusterGroup = L.markerClusterGroup();
  var markers = [];              // parallel array to companies[]
  // clusterGroup and markers[] are populated; map.addLayer(clusterGroup) called
  // legend.addTo(map) called last — new JS goes BEFORE </script>

Default CircleMarker style (what "normal" means for reset):
  radius: 8, fillColor: getColor(c.ISP), color: "#ffffff", weight: 1, fillOpacity: 0.85

HDR_LAT = "Lat", HDR_LON = "Lon", HDR_GEOCODED = "GeocodedAt" (mod_Config.bas constants
baked into JSON keys — JS side uses these exact string values)

AppendLine signature:
  Private Sub AppendLine(ByRef sb As String, ByVal line As String)

Insertion point: Add new AppendLine blocks after the existing
  AppendLine sb, "legend.addTo(map);"
line and before the closing
  AppendLine sb, "</script></body></html>"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: State variables, haversine, clearSelection, applySelection, buildPopupHtml</name>
  <files>src/mod_MapBuilder.bas</files>
  <read_first>
    src/mod_MapBuilder.bas — read the full file to see current AppendLine structure, where the legend.addTo(map) line is, and the exact style values used for markers; use the insertion point described in the interfaces block above.
  </read_first>
  <action>
    Inside BuildMapHtml(), after the AppendLine for "legend.addTo(map);" and before the closing "</script>" line, add a new AppendLine block containing this JavaScript:

    State variables (use JS var, not let/const — IE compatibility not needed but var is consistent with Phase 2 style):
      var selectedMarker = null;
      var selectedCompany = null;
      var selectedISP = null;
      var radiusCircle = null;
      var currentField = 'ISP';
      var fieldColorMap = {};
      var fieldNames = [];
      var hiddenValues = {};

    haversine function — Earth radius 3958.8 miles (D-01). Use single-quoted JS strings throughout to avoid VBA double-quote escaping:
      function haversine(lat1, lon1, lat2, lon2) {
        var R = 3958.8;
        var dLat = (lat2-lat1)*Math.PI/180;
        var dLon = (lon2-lon1)*Math.PI/180;
        var a = Math.sin(dLat/2)*Math.sin(dLat/2) +
                Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*
                Math.sin(dLon/2)*Math.sin(dLon/2);
        return R*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));
      }

    buildPopupHtml function — show all fields except 'Lat' and 'Lon' (D-14). Keys shown as-is. Build an HTML table:
      function buildPopupHtml(company) {
        var POPUP_EXCLUDE = ['Lat','Lon'];
        var rows = '';
        Object.keys(company).forEach(function(k) {
          if (POPUP_EXCLUDE.indexOf(k) === -1) {
            rows += '<tr><td style="font-weight:bold;padding-right:8px">' + k + '</td><td>' + company[k] + '</td></tr>';
          }
        });
        return '<table style="border-collapse:collapse;font-size:13px">' + rows + '</table>';
      }

    clearSelection function — remove radiusCircle from map, reset all markers to default style, close any open popup, null out state:
      function clearSelection() {
        if (radiusCircle) { map.removeLayer(radiusCircle); radiusCircle = null; }
        markers.forEach(function(m, i) {
          m.setRadius(8);
          m.setStyle({ fillColor: ispColorMap[companies[i].ISP] || OTHER_COLOR, color: '#ffffff', weight: 1, fillOpacity: 0.85 });
        });
        map.closePopup();
        selectedMarker = null;
        selectedCompany = null;
        selectedISP = null;
      }

    IMPORTANT: In clearSelection, the fillColor restore uses ispColorMap[companies[i].ISP] — this is the initial ISP color. After Plan 02 introduces currentField and fieldColorMap, clearSelection will need updating. For now, use ispColorMap directly since currentField is always 'ISP' at this point.

    applySelection function — takes (marker, company), reads slider value from document.getElementById('radiusSlider').value, applies all visual changes:
      1. If selectedMarker is the same as marker: call clearSelection() and return (toggle deselect, D-10).
      2. Call clearSelection() to reset previous selection.
      3. Store selectedMarker = marker, selectedCompany = company, selectedISP = company.ISP.
      4. Open popup using L.popup().setContent(buildPopupHtml(company)).setLatLng(marker.getLatLng()).openOn(map).
      5. Apply selected marker style (D-04): marker.setRadius(12); marker.setStyle({ fillColor: '#ffffff', color: ispColorMap[company.ISP] || OTHER_COLOR, weight: 2, fillOpacity: 0.85 });
      6. Draw radius circle (D-07): var sliderVal = parseFloat(document.getElementById('radiusSlider').value); var radiusMeters = sliderVal * 1609.34; radiusCircle = L.circle(marker.getLatLng(), { radius: radiusMeters, color: '#0078ff', weight: 1, fillOpacity: 0.05, interactive: false }).addTo(map);
      7. Iterate markers[] to apply neighbor highlighting. Skip the selected marker itself. Skip any marker where hiddenValues[companies[i].ISP] is true (D-13 — hidden markers stay hidden). For each remaining marker, compute dist = haversine(company.Lat, company.Lon, companies[i].Lat, companies[i].Lon). If dist <= sliderVal (neighbor): m.setRadius(8); m.setStyle({ fillColor: ispColorMap[companies[i].ISP] || OTHER_COLOR, color: companies[i].ISP === company.ISP ? '#ffffff' : '#ffffff', weight: companies[i].ISP === company.ISP ? 3 : 1, fillOpacity: 0.85 }). If NOT neighbor: m.setRadius(8); m.setStyle({ fillOpacity: 0.15 });

    NOTE on D-06 neighbor styles: Same-ISP neighbors get color '#ffffff' weight 3. Other-ISP neighbors get color '#ffffff' weight 1. Both retain their ISP fillColor. The color field in setStyle is the BORDER color; the fillColor is the ISP color from ispColorMap.
  </action>
  <verify>
    <automated>
      Open the generated geoviz_map.html in a browser after running the GenerateMap macro. In browser DevTools console, run:
        typeof haversine === 'function'
        typeof clearSelection === 'function'
        typeof applySelection === 'function'
        typeof buildPopupHtml === 'function'
        typeof selectedMarker
      All should return truthy/defined without errors. Also verify:
        haversine(40.7128, -74.0060, 34.0522, -118.2437)
      Should return approximately 2445 (miles between NYC and LA).
    </automated>
  </verify>
  <acceptance_criteria>
    - src/mod_MapBuilder.bas contains AppendLine calls for "function haversine", "function clearSelection", "function applySelection", "function buildPopupHtml"
    - haversine uses var R = 3958.8 (miles, not km)
    - applySelection calls marker.setRadius(12) and marker.setStyle({...}) as separate calls — never setStyle({radius:12})
    - applySelection creates L.circle with radius: sliderVal * 1609.34 (not sliderVal directly)
    - clearSelection calls map.closePopup() and sets selectedMarker = null
    - Non-neighbor markers get fillOpacity: 0.15 via setStyle
    - Same-ISP neighbors get weight: 3; other-ISP neighbors get weight: 1; both via setStyle
    - hiddenValues check present in applySelection neighbor loop: markers hidden by legend toggle are skipped
    - buildPopupHtml excludes 'Lat' and 'Lon' keys via POPUP_EXCLUDE array
    - All JS strings inside AppendLine use single quotes (not double quotes) wherever possible
  </acceptance_criteria>
  <done>BuildMapHtml() contains state vars block, haversine, buildPopupHtml, clearSelection, and applySelection as AppendLine sequences. VBA file compiles without error (check via Excel VBA editor). Browser console shows all four functions defined and haversine returns ~2445 for NYC-to-LA coordinates.</done>
</task>

<task type="auto">
  <name>Task 2: Radius slider Leaflet control and event bindings</name>
  <files>src/mod_MapBuilder.bas</files>
  <read_first>
    src/mod_MapBuilder.bas — read the full file again after Task 1 to see the current state; find the block just appended and add this new block immediately after it, still before the closing "</script>" line.
  </read_first>
  <action>
    After the applySelection block from Task 1 (still inside BuildMapHtml, before the closing "</script>"), add another AppendLine block with:

    Radius slider Leaflet control (D-03). Use L.control({position:'topleft'}):
      var radiusControl = L.control({position:'topleft'});
      radiusControl.onAdd = function(map) {
        var div = L.DomUtil.create('div', 'radius-control');
        div.style.cssText = 'background:white;padding:8px 12px;border-radius:4px;font-size:13px;box-shadow:0 1px 5px rgba(0,0,0,.3);min-width:160px';
        div.innerHTML = '<label style="display:block;margin-bottom:6px"><strong>Radius: <span id="radiusLabel">0.5</span> mi</strong></label>'
                      + '<input id="radiusSlider" type="range" min="0.1" max="5" step="0.1" value="0.5" style="width:140px">'
                      + '<div id="radiusHint" style="margin-top:6px;font-size:11px;color:#888">Click a company to see neighbors</div>';
        L.DomEvent.disableClickPropagation(div);
        L.DomEvent.disableScrollPropagation(div);
        return div;
      };
      radiusControl.addTo(map);

    CRITICAL: L.DomEvent.disableClickPropagation(div) MUST be called on the div — without it, slider interaction fires map.on('click', clearSelection) and immediately clears the selection (Research Pitfall 1).

    After radiusControl.addTo(map), bind the slider input event:
      document.getElementById('radiusSlider').addEventListener('input', function() {
        document.getElementById('radiusLabel').textContent = this.value;
        if (selectedMarker) { applySelection(selectedMarker, selectedCompany); }
      });

    Bind marker click handlers — use stopPropagation to prevent map click from also firing:
      markers.forEach(function(marker, i) {
        marker.on('click', function(e) {
          L.DomEvent.stopPropagation(e);
          applySelection(marker, companies[i]);
        });
      });

    Bind map background click to clearSelection (D-09):
      map.on('click', clearSelection);

    Show/hide the hint div based on selection state. Update clearSelection (add this after the existing clearSelection function) to show the hint:
      Actually, handle this inside the applySelection and clearSelection bodies already written in Task 1. In applySelection, hide the hint: var hint = document.getElementById('radiusHint'); if (hint) hint.style.display = 'none';. In clearSelection, show the hint: var hint = document.getElementById('radiusHint'); if (hint) hint.style.display = 'block';.

    Because clearSelection was written in Task 1 without the hint logic, add two AppendLine lines that redefine or extend it. The cleanest approach: add a separate function updateHint(visible) called from both applySelection and clearSelection. BUT since both functions are already defined as AppendLine blocks, the simplest approach is to define a helper after the functions:
      function setHint(show) {
        var h = document.getElementById('radiusHint');
        if (h) h.style.display = show ? 'block' : 'none';
      }
    Then call setHint(false) at the start of applySelection (after the toggle-deselect check), and setHint(true) at the end of clearSelection. If it is simpler to add setHint inline at the call site via AppendLine, do so.

    ALTERNATIVELY (simpler): since document.getElementById('radiusHint') returns null until the control is added to the map, both clearSelection and applySelection can call setHint safely even if they are defined before radiusControl.addTo(map) — the function just no-ops when the element is not yet in the DOM. Just define setHint() after Task 1's block and before the radiusControl block, then reference it from applySelection and clearSelection using AppendLine patching. Actually the cleanest approach is to embed the hint toggle directly inside applySelection and clearSelection during Task 1. Since Task 2 follows Task 1, update the applySelection and clearSelection AppendLine blocks to include setHint calls, or just inline the getElementById calls.

    Preferred approach: Keep it simple. In this Task 2 block, after binding the slider, add:
      function setHint(show) { var h = document.getElementById('radiusHint'); if (h) h.style.display = show ? 'block' : 'none'; }
    Then add the following two additional AppendLine lines that patch clearSelection and applySelection by defining setHint and relying on the JS engine to resolve it at call time (not parse time), which is valid since all code is in the same script block. The original clearSelection function already closes over setHint by name. Just call setHint(true) at the END of clearSelection's body and setHint(false) near the top of applySelection's body. Accomplish this by editing the Task 1 AppendLine blocks to include those calls. If the executor is writing all AppendLine blocks fresh in one pass, include the setHint calls in-place during Task 1 writing.

    Executor discretion: The simplest implementation is to write Task 1 and Task 2 AppendLine blocks in a single editing pass, ensuring setHint is defined before it is called. Refer to the RESEARCH.md code examples for verified patterns.
  </action>
  <verify>
    <automated>
      Run GenerateMap macro and open geoviz_map.html. In browser DevTools console:
        document.getElementById('radiusSlider') !== null   // should be true
        document.getElementById('radiusLabel').textContent  // should be "0.5"
      Manual check: drag slider from 0.1 to 5 — label updates. Click a marker — circle appears and slider drag updates it live. Click map background — circle disappears and markers restore.
    </automated>
  </verify>
  <acceptance_criteria>
    - radiusControl uses L.control({position:'topleft'}) — NOT an absolutely-positioned div appended to document.body
    - L.DomEvent.disableClickPropagation(div) called inside onAdd (verify: AppendLine contains "disableClickPropagation")
    - L.DomEvent.disableScrollPropagation(div) called inside onAdd
    - radiusSlider has min="0.1", max="5", step="0.1", value="0.5" (D-02)
    - Slider input event listener calls applySelection(selectedMarker, selectedCompany) only when selectedMarker is not null
    - marker.on('click') handler calls L.DomEvent.stopPropagation(e) before applySelection
    - map.on('click', clearSelection) present
    - VBA file compiles without error in Excel VBA editor
  </acceptance_criteria>
  <done>GenerateMap macro produces a valid HTML file. Browser shows top-left radius panel with label "Radius: 0.5 mi" and a range slider. Clicking a marker opens popup, draws blue circle, fades non-neighbors. Dragging slider resizes circle and updates highlights. Clicking map background clears all selection state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VBA string → HTML/JS | Company data is embedded as JSON via VBA string concatenation; XSS possible if company fields contain HTML special characters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | buildPopupHtml — company field values rendered as innerHTML | accept | Local file only; data source is the user's own Excel sheet; no external input; risk is negligible |
| T-03-02 | Information Disclosure | geoviz_map.html written to local disk | accept | Single-user tool; no sensitive data flow beyond what is already in the Excel workbook |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 adds no package installs; CDN libraries unchanged from Phase 2 |
</threat_model>

<verification>
After both tasks are complete, run GenerateMap macro and verify in browser:

1. Top-left panel shows "Radius: 0.5 mi" label and slider
2. Clicking a marker: popup appears with all fields except Lat and Lon; blue circle drawn; non-neighbors faded to ~15% opacity; same-ISP neighbors have thick white border (weight 3); selected marker is larger (radius 12) with white fill and ISP-colored border
3. Dragging slider from 0.5 to 2.0: circle expands and neighbors update live
4. Clicking map background: circle removed, all markers restore to default style, popup closes
5. Clicking a second marker: first selection clears, second activates
6. Clicking selected marker again: selection clears (toggle deselect)
</verification>

<success_criteria>
All six verification behaviors above pass manual browser testing. VBA module compiles without errors. haversine(40.7128, -74.0060, 34.0522, -118.2437) returns a value between 2440 and 2450 miles in the browser console.
</success_criteria>

<output>
Create `.planning/phases/03-interactivity/03-01-SUMMARY.md` when done.
</output>
